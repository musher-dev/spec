# A specVersion this schema release does not implement. A validator must
# reject rather than attempt a best-effort interpretation.
specVersion: v99
kind: COMPONENT
metadata:
  version: 1
spec:
  workload:
    kind: SERVICE
    source:
      type: IMAGE
      ref: nginx:1.29.4-alpine
