# An absent graph and an empty one fail differently, and both fail. `spec`
# without `components` is a missing required property rather than a bound
# violation.
specVersion: v1
kind: BLUEPRINT
metadata:
  slug: acme-wiki
  version: 1
spec:
  parameters: {}
