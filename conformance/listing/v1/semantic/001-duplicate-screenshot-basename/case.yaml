# Two distinct files, two distinct paths, one basename. Published assets are
# addressed by basename, so these are one file — and without the rule the
# second screenshot silently replaces the first.
specVersion: v1
kind: LISTING
metadata:
  slug: postgres
  version: 1
spec:
  listingKind: COMPONENT
  displayName: PostgreSQL
  summary: The open-source relational database
  category: INFRASTRUCTURE
  lifecycleStage: STABLE
  screenshots:
    - file: media/desktop/overview.png
    - file: media/mobile/overview.png
