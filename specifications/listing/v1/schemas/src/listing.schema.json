{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.musher.dev/listing/v1/listing",
  "title": "Musher Listing Document",
  "description": "The catalog storefront entry for a blueprint or component.",
  "$comment": "Structural and type validation only — cross-document reference resolution, slug/directory agreement, and platform capability checks are separate validation layers. See spec.md.",
  "additionalProperties": false,
  "properties": {
    "kind": {
      "const": "LISTING",
      "description": "Document kind discriminator. Must be LISTING.",
      "type": "string"
    },
    "metadata": {
      "$ref": "#/$defs/ListingMetadata"
    },
    "spec": {
      "$ref": "#/$defs/ListingSpec"
    },
    "specVersion": {
      "description": "Version of the Musher specification this document is written against.",
      "enum": [
        "v1"
      ],
      "type": "string"
    }
  },
  "required": [
    "specVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "type": "object",
  "$defs": {
    "ListingMetadata": {
      "additionalProperties": false,
      "description": "Identity block of a listing document. The same shape carries on the sibling blueprint document, and the two must agree.",
      "properties": {
        "slug": {
          "description": "Item slug — must equal the item's directory name.",
          "pattern": "^[a-z][a-z0-9-]{0,61}[a-z0-9]$",
          "type": "string"
        },
        "version": {
          "description": "Item version — must agree between blueprint.yaml and listing.yaml.",
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "slug",
        "version"
      ],
      "type": "object"
    },
    "ListingScreenshot": {
      "additionalProperties": false,
      "description": "One storefront screenshot declared as a relative media path.",
      "properties": {
        "caption": {
          "anyOf": [
            {
              "maxLength": 280,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Accessible caption shown beneath the screenshot, or null when none."
        },
        "file": {
          "$comment": "Lookahead-free so it compiles under RE2 as well as ECMA-262. A segment must begin with a letter or digit, which is how `.` and `..` are excluded without a negative lookahead; the extension alternation is spelled in character classes because a JSON Schema pattern carries no case-insensitive flag. See spec.md §5.",
          "description": "Path of the screenshot asset under the item's media/ directory, relative to the item root. Must be a .png, .jpg, .jpeg or .webp below media/.",
          "maxLength": 512,
          "minLength": 1,
          "pattern": "^media/(?:[A-Za-z0-9][A-Za-z0-9._-]*/)*[A-Za-z0-9][A-Za-z0-9._-]*\\.(?:[Pp][Nn][Gg]|[Jj][Pp][Gg]|[Jj][Pp][Ee][Gg]|[Ww][Ee][Bb][Pp])$",
          "type": "string"
        }
      },
      "required": [
        "file"
      ],
      "type": "object"
    },
    "ListingSpec": {
      "additionalProperties": false,
      "description": "The ``spec`` block of a listing document — how the item is presented,\ncategorised, and discovered on the storefront.\n\nIdentity lives in ``metadata``, not here. ``icon`` and\n``screenshots[].file`` are paths relative to the listing document.",
      "properties": {
        "category": {
          "description": "Storefront category the listing is browsed under.",
          "enum": [
            "MEDIA",
            "PRODUCTIVITY",
            "AI_ML",
            "DEVELOPER_TOOLS",
            "INFRASTRUCTURE",
            "COMMUNICATION",
            "AUTOMATION",
            "NETWORKING",
            "STORAGE",
            "SECURITY",
            "GAMING"
          ],
          "type": "string"
        },
        "description": {
          "anyOf": [
            {
              "maxLength": 20000,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Long-form Markdown storefront description, or null when none."
        },
        "displayName": {
          "description": "Storefront title shown to prospective installers.",
          "maxLength": 200,
          "minLength": 1,
          "type": "string"
        },
        "homepageUrl": {
          "anyOf": [
            {
              "maxLength": 2048,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Project homepage URL, or null when none."
        },
        "icon": {
          "anyOf": [
            {
              "$comment": "The same grammar ListingScreenshot.file carries; see the note there.",
              "maxLength": 512,
              "pattern": "^media/(?:[A-Za-z0-9][A-Za-z0-9._-]*/)*[A-Za-z0-9][A-Za-z0-9._-]*\\.(?:[Pp][Nn][Gg]|[Jj][Pp][Gg]|[Jj][Pp][Ee][Gg]|[Ww][Ee][Bb][Pp])$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Path of the listing's icon under the item's media/ directory (e.g. media/icon.png), relative to the item root, or null for no icon."
        },
        "license": {
          "anyOf": [
            {
              "maxLength": 256,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "License identifier for the listed software, or null when unspecified."
        },
        "lifecycleStage": {
          "description": "Maturity stage shown on the storefront: STABLE, BETA, EXPERIMENTAL, or SUNSET.",
          "enum": [
            "STABLE",
            "BETA",
            "EXPERIMENTAL",
            "SUNSET"
          ],
          "type": "string"
        },
        "listingKind": {
          "description": "Storefront classification: BLUEPRINT (curated composition) or COMPONENT (single building block).",
          "enum": [
            "BLUEPRINT",
            "COMPONENT"
          ],
          "type": "string"
        },
        "screenshots": {
          "description": "Storefront screenshots in display order, declared as media paths.",
          "items": {
            "$ref": "#/$defs/ListingScreenshot"
          },
          "type": "array"
        },
        "sourceRepoUrl": {
          "anyOf": [
            {
              "maxLength": 2048,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Source repository URL, or null when none."
        },
        "summary": {
          "description": "One-line storefront tagline summarising the listing.",
          "maxLength": 280,
          "minLength": 1,
          "type": "string"
        },
        "supportUrl": {
          "anyOf": [
            {
              "maxLength": 2048,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Support or documentation URL, or null when none."
        },
        "tags": {
          "description": "Free-form discovery tags applied to the listing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "listingKind",
        "displayName",
        "summary",
        "category",
        "lifecycleStage"
      ],
      "type": "object"
    }
  }
}
