# The same rule reached the other way. The block is present and empty, so this
# is a value the schema rejects rather than a field it misses — the two are
# separate diagnostics because they read differently to an author. One forgot
# the block; the other wrote it and put nothing in it.
specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: nginx:1.29.4-alpine
    endpoints: {}
